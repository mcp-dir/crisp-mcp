# Ferramentas

Crisp expõe 1 ferramenta (todas somente leitura).

### 1. `search_tools`
**Input**: `action` (opcional), `query` (opcional), `min_score` (opcional), `force_reindex` (opcional), `queries` (opcional), `tool_id` (opcional), `arguments` (opcional)

Single entrypoint for MCP catalog.

## Prompts de exemplo

```
Liste as conversas abertas do meu website Crisp
Quais contatos novos entraram esta semana?
Mostre os artigos publicados na base de conhecimento
```
